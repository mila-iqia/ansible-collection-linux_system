---
# Exclude paths that don't need linting
exclude_paths:
  - .github/

# Trigger warning instead of error
warn_list:
  - galaxy[no-changelog]  # Warn when changelog file is missing
  - no-changed-when  # Warn when tasks don't use changed_when
