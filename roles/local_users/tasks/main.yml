---
- name: Ensure required packages are installed
  ansible.builtin.package:
    name:
      - curl
      - sudo

# Configure user for automation
- import_tasks: automation.yml
  tags:
    - local_automation
    - local_users

# Add local users
- import_tasks: admins.yml
  tags:
    - local_admins
    - local_users

- import_tasks: operators.yml
  tags:
    - local_operators
    - local_users

- import_tasks: service_accounts.yml
  tags:
    - local_service_accounts
    - local_users
