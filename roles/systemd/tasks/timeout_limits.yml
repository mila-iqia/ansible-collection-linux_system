---
- name: Template a file to /etc/systemd/system.conf.d/
  ansible.builtin.template:
    src: timeout_limits.conf.j2
    dest: /etc/systemd/system.conf.d/10-timeout-limits.conf
    owner: root
    group: root
    mode: '0644'
  notify: Reload daemon
